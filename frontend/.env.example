VITE_API_URL="http://localhost:4000"

# Same Google OAuth Client ID as backend/.env's GOOGLE_CLIENT_ID. Used by Google Identity
# Services on the frontend to render the "Sign in with Google" button.
VITE_GOOGLE_CLIENT_ID=""
